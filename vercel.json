{
  "buildCommand": "cd careercopilot-web && npm install && npx vite build",
  "outputDirectory": "careercopilot-web/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}